interface CheckboxProps {
    onPressButton?: () => void;
    containerStyle?: any; // Define the type for containerStyle
    isSelected?: any; // Define the type for isSelected
    tintColor?: string;
    unckeck?: boolean; // Corrected typo, should be "unchecked"
  }