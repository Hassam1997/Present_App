/** @format */

interface IPropsgiftProducts {
	item: {
		image: isTypeObject;
		brandIcon: isTypeObject;
		price: isTypeNumber;
		productDescription: isTypeString;
		wishList: boolean;
	};
	index: number;
}
